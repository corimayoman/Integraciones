{
  "name": "i4g-integration-tracker",
  "version": "1.0.0",
  "description": "AMS Integration Tracker Dashboard — visualización del estado de integración de empresas adquiridas",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:coverage": "vitest --run --coverage"
  },
  "devDependencies": {
    "fast-check": "^4.1.1",
    "jsdom": "^29.0.2",
    "vitest": "^3.2.1"
  }
}
